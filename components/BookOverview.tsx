export default function BookOverview({ title, author, description, coverImage }: {}) {
  return (
    <section className="book-overview">
      <div className="flex flex-1 flex-col gap-5">
        <h1>A long boog title</h1>
      </div>
    </section>
  );
}
